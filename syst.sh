#!/bin/bash

set -e

sudo apt update
sudo apt install -y budgie-desktop gnome-session gnome-shell gnome-session-flashback kde-plasma-desktop ubuntu-desktop mate-desktop-environment neofetch btop wine ffmpeg
